import { DocumentFrame } from '../../components/DocumentFrame'

/** Placeholder until the real mockup lands. */
export default function CertificateLines() {
  return (
    <DocumentFrame title="CertificateLines" meta="mockup pending">
      <div className="mono" style={{ padding: 24, minHeight: 240 }}>
        CertificateLines: pending
      </div>
    </DocumentFrame>
  )
}
