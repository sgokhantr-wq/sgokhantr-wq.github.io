import { DocumentFrame } from '../../components/DocumentFrame'

/** Placeholder until the real mockup lands. */
export default function MonthBoard() {
  return (
    <DocumentFrame title="MonthBoard" meta="mockup pending">
      <div className="mono" style={{ padding: 24, minHeight: 240 }}>
        MonthBoard: pending
      </div>
    </DocumentFrame>
  )
}
